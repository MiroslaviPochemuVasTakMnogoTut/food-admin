import { PointFromStringPipe } from './point-from-string.pipe';

describe('PointFromStringPipe', () => {
  it('should be defined', () => {
    expect(new PointFromStringPipe()).toBeDefined();
  });
});
